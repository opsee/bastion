[Unit]
Description=Bastion - Monitor
After=docker.service
Requires=docker.service nsqd.service

[Service]
User=core
TimeoutStartSec=0
ExecStartPre=-/usr/bin/docker kill %p
ExecStartPre=-/usr/bin/docker rm %p
ExecStartPre=/usr/bin/docker pull quay.io/opsee/bastion:latest
ExecStart=/usr/bin/docker run --name %p -p 4000:4000 --link nsqd:nsqd quay.io/opsee/bastion:latest /monitor
ExecStop=/usr/bin/docker stop %p

[Install]
WantedBy=multi-user.target
